{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && npx @biomejs/biome check --write \"$f\" 2>/dev/null || true; }",
            "timeout": 30,
            "statusMessage": "Auto-formatting..."
          }
        ]
      }
    ]
  }
}
